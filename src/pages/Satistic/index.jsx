import React from 'react';
import PropTypes from 'prop-types';

const SatisticPage = props => {
    return (
        <div>123</div>
    );
};

SatisticPage.propTypes = {
    
};

export default SatisticPage;